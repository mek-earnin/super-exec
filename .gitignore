# super-exec local working dirs (plans, handoff, scratch) — never committed
.super-exec/

# research mined from past conversations — local-only, may contain internal data
research/

# macOS Finder metadata
.DS_Store
**/.DS_Store
